# RemUp 扩展功能测试文件

## 自动换行测试 (!: 重点) (>: #语法高亮)

这是一段很长的文本内容，用于测试自动换行功能是否正常工作。如果配置正确，这段文字会在到达编辑器右边界时自动换行，而不是出现横向滚动条。这是 RemUp v1.1.0 版本的新特性之一，让你获得与 Markdown 一致的舒适编辑体验。

### 区域一：基础测试

继续测试长文本的换行效果。RemUp 是一个轻量级标记语言，旨在构建"学习 - 理解 - 再学习"的记忆闭环。通过 VSCode 扩展的优化配置，你现在可以享受更加流畅的写作体验。

### 区域二：代码示例

```python
def test_remup():
    """测试 RemUp 功能"""
    print("RemUp 扩展工作正常！")
    return True
```

## 注卡语法测试

`自动换行`[v1.1.0 新增功能] ^编辑器设置
`语法高亮`[核心功能] ^基础支持

## 标签系统测试

(!: 重要提示) 请确保已安装最新版本的 RemUp 扩展。

(>: #相关文档) 查看 README.md 获取详细配置说明。

(*: ⭐⭐⭐⭐⭐) 推荐指数：五颗星！

## 折叠功能测试

尝试点击左侧的折叠箭头，测试卡片和区域的折叠功能是否正常工作。

---

### 空白区域名测试

这是一个没有标题的区域，用于测试空区域名的渲染效果。

---

## 结尾总结

通过以上测试，你可以验证 RemUp VSCode 扩展的各项功能：

1. ✅ **自动换行** - 长文本自动折行，无需横向滚动
2. ✅ **语法高亮** - 所有语法元素正确着色
3. ✅ **折叠功能** - 卡片和区域可以折叠/展开
4. ✅ **注卡语法** - 悬停批注正常显示
5. ✅ **标签系统** - 不同类型标签颜色区分

如果所有测试都通过，恭喜你！RemUp 扩展已经完美配置并正常工作了！🎉

/+>